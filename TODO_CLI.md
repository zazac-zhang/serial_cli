# CLI 后端待完成功能清单

## 总体进度：约 80% 完成

## ✅ 已完成功能

### 1. 串口管理 (90%)
- 端口列表、打开、关闭
- 数据收发
- 端口配置（波特率、数据位、停止位、校验位）
- 端口状态查询
- ⚠️ DTR/RTS 控制（框架完成，平台特定实现待完善）

### 2. 协议系统 (95%)
- 内置协议：Modbus RTU/ASCII、AT Command、Line
- 自定义 Lua 协议支持
- 协议注册表和工厂
- 协议验证器
- 协议热加载和监控

### 3. Lua 集成 (95%)
- LuaJIT 引擎集成
- 完整的 Lua API 绑定
- 标准库工具函数（hex 编解码、字符串转换等）
- 脚本执行器

### 4. CLI 接口 (85%)
- 单命令模式
- 交互式 shell（功能完整）
- 协议管理命令
- 配置显示

### 5. GUI 后端 (90%)
- 完整的 Tauri 命令集
- 事件系统（数据收发事件）
- 后台数据嗅探
- 端口健康检查
- 系统托盘支持

### 6. 任务调度 (80%)
- TaskQueue 和 TaskExecutor
- 任务监控
- 多种任务类型支持

## ⚠️ 部分完成/待集成功能

### 1. 嗅探功能 (60%)
- ✅ SerialSniffer 模块已实现
- ✅ GUI 后端有实时数据嗅探
- ❌ CLI sniff 命令需要集成现有模块

### 2. 批处理功能 (50%)
- ✅ BatchRunner 模块存在
- ❌ CLI 集成不完整
- ❌ 批处理脚本列表管理

### 3. 配置管理 (60%)
- ✅ 配置加载和默认值
- ✅ 配置显示
- ❌ 运行时配置设置未实现
- ❌ 配置保存未实现
- ❌ 配置重置未实现

## 🔧 待完成功能

### 高优先级

#### 1. 完善 DTR/RTS 控制
**位置**: `src/serial_core/port.rs:324, 336`

**当前状态**: 框架完成，但平台特定实现标记为 TODO

**需要补充**:
- Windows 平台：使用 `serialport` 的 set_dtr/set_rts 方法
- macOS/Linux 平台：使用 termios/ioctl 系统调用
- 跨平台抽象层，统一 API

**为什么需要补充**:
- DTR/RTS 是串口通信中的重要硬件控制信号
- 用于控制外部设备（如 RS-485 转换器的发送/接收方向）
- 某些工业设备需要特定的 DTR/RTS 时序才能正常工作
- 当前的占位实现无法满足实际硬件控制需求

#### 2. 运行时配置修改
**位置**: `src/main.rs` handle_config_command

**当前状态**: 配置命令存在但只输出提示信息

**是否必要**: **是，非常必要**

**原因**:
1. **用户体验**: 用户无需手动编辑 TOML 文件
2. **脚本自动化**: Lua 脚本可以动态调整配置
3. **GUI 集成**: GUI 需要运行时修改配置的能力
4. **配置验证**: 修改时可以立即验证配置有效性
5. **配置持久化**: 修改后的配置需要保存到文件

**需要实现**:
- `config set <key> <value>` 命令
- `config save [path]` 命令
- `config reset` 命令
- 配置验证逻辑

#### 3. 配置读取和设置完善
**位置**: `src/config.rs`

**当前状态**: 支持加载，缺少运行时设置和保存

**需要补充**:
- 运行时配置修改 API
- 配置保存到文件
- 配置重置为默认值
- 配置验证逻辑
- 配置变更通知机制

**为什么需要完善**:
- 形成配置管理的完整闭环
- 确保配置修改后能持久化
- 避免配置不一致问题
- 提供配置回滚能力

#### 4. IoLoop 集成
**位置**: `src/serial_core/io_loop.rs` 需要集成到端口管理

**当前状态**: IoLoop 模块已实现但未集成

**需要补充**:
- 将 IoLoop 集成到 PortManager
- 为每个打开的端口启动 IoLoop
- 实现异步 I/O 事件处理
- 回调机制通知数据到达

**为什么需要**:
- 当前同步读取方式效率较低
- IoLoop 提供异步事件驱动模型
- 支持多端口并发处理
- 为未来高级功能（如自动重连）打基础

#### 5. 批处理 CLI 集成
**位置**: `src/main.rs` handle_batch_command

**当前状态**: 命令存在但只输出提示信息

**需要补充**:
- 实现 `batch run <script>` 命令
- 实现 `batch list` 命令
- 批处理脚本格式定义
- 错误处理和日志记录

**为什么需要**:
- 支持自动化测试场景
- 批量操作多个串口设备
- 定时任务执行
- 简化重复性工作流程

#### 6. Sniff CLI 集成
**位置**: `src/main.rs` handle_sniff_command

**当前状态**: 命令存在但只输出提示信息

**需要补充**:
- 实现 `sniff start` 命令，集成现有 SerialSniffer
- 实现 `sniff stats` 命令
- 实现 `sniff save` 命令
- 数据包捕获显示

**为什么需要**:
- 调试串口通信问题
- 分析协议数据包
- 记录通信日志
- 性能分析

#### 7. Lua 参数传递
**位置**: `src/lua/executor.rs:38`

**当前状态**: 标记为 TODO

**需要补充**:
- 支持 Lua 脚本接收命令行参数
- 在 Lua 中通过 `arg` 表或全局变量访问参数
- 参数类型转换（字符串 -> Lua 值）

**为什么需要**:
- 使 Lua 脚本更加灵活和可重用
- 同一脚本可以处理不同的输入参数
- 支持自动化脚本传参
- 与传统脚本语言（如 bash、python）保持一致

## 实现优先级

1. **P0 (立即实现)**: DTR/RTS 控制、配置管理完善
2. **P1 (近期实现)**: IoLoop 集成、Lua 参数传递
3. **P2 (中期实现)**: Sniff CLI 集成、批处理 CLI 集成

## 预期完成时间

- P0 功能: 2-3 天
- P1 功能: 3-4 天
- P2 功能: 2-3 天

**总计**: 约 1-2 周完成所有待实现功能
